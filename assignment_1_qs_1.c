#include<stdio.h>
int main()
{
    int first_value,middle_value,last_value;

    printf("Enter three numbers :");
    scanf("%d %d %d",&first_value,&middle_value,&last_value);

    printf("First Value = %d , Last Value = %d ",first_value,last_value);
    return 0;

}
