#include <stdio.h>

int main() {
    char str[40];
    // scanf ("%s",str); space can considered
     gets(str);   /*full sentence can be considered*/
     printf("Input was : %s",str);

    return 0;
}
